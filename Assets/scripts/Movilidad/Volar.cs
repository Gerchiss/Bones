using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Volar : Movilidad
{
    // Start is called before the first frame update
    void Start()
    {
        velocidadH = 1;
        velocidadV = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
